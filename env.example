# ==========================================
# LinkedIn Recommendation Writer - Environment Variables
# ==========================================

# ==========================================
# REQUIRED API KEYS
# ==========================================

# GitHub Personal Access Token
# Get from: https://github.com/settings/tokens
# Required scopes: repo, user
GITHUB_TOKEN=your_github_token_here

# Google Gemini AI API Key
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ==========================================
# DATABASE CONFIGURATION
# ==========================================
# For Railway: These will be auto-configured when you add PostgreSQL plugin
# For local development: Set these manually

# PostgreSQL Database URL (Railway will provide this)
DATABASE_URL=postgresql://postgres:password@localhost:5432/github_recommender

# ==========================================
# REDIS CONFIGURATION
# ==========================================
# For Railway: This will be auto-configured when you add Redis plugin
# For local development: Set this manually

# Redis URL (Railway will provide this)
REDIS_URL=redis://localhost:6379/0

# ==========================================
# APPLICATION CONFIGURATION
# ==========================================

# Environment (development/staging/production)
ENVIRONMENT=production

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_DEBUG=false
API_RELOAD=false
API_WORKERS=4

# Logging
LOG_LEVEL=INFO

# Database Pool Configuration
DATABASE_POOL_SIZE=10
DATABASE_MAX_OVERFLOW=20
DATABASE_POOL_TIMEOUT=30

# Redis Configuration
REDIS_TIMEOUT=5
REDIS_DEFAULT_TTL=3600

# AI Configuration
GEMINI_MODEL=gemini-2.5-flash-lite
GEMINI_TEMPERATURE=0.7
GEMINI_MAX_TOKENS=2048

# Rate Limiting
RATE_LIMIT_REQUESTS_PER_MINUTE=60
RATE_LIMIT_BURST_SIZE=10

# Feature Flags
ENABLE_RATE_LIMITING=true
ENABLE_METRICS=false
ENABLE_TRACING=false

# ==========================================
# SECURITY
# ==========================================

# Secret Key for JWT tokens (MUST be set in production)
# This ensures JWT tokens remain valid across application restarts
# Generate with: openssl rand -hex 32
SECRET_KEY=your_secret_key_here

# CORS Origins (for production, set to your frontend domain)
# ALLOWED_ORIGINS=https://your-frontend-domain.com

# ==========================================
# FRONTEND CONFIGURATION
# ==========================================

# API Base URL (set this in your frontend Railway service)
VITE_API_BASE_URL=https://your-backend-service-url.railway.app
VITE_API_TIMEOUT=30000

# Application Configuration
VITE_APP_NAME="LinkedIn Recommendation Writer"
VITE_APP_VERSION="1.0.0"

# Feature Flags
VITE_ENABLE_ADVANCED_FEATURES=true
VITE_ENABLE_DEBUG_MODE=false

# UI Configuration
VITE_DEFAULT_THEME="light"
VITE_MAX_RECOMMENDATIONS_PER_DAY=3

# ==========================================
# DEPLOYMENT NOTES
# ==========================================
#
# 1. For Railway deployment:
#    - Copy this file to .env
#    - Railway will automatically set DATABASE_URL and REDIS_URL
#    - Add your API keys in Railway dashboard
#
# 2. For local development:
#    - Set DATABASE_URL and REDIS_URL manually
#    - Use docker-compose.yml for local setup
#
# 3. Required Railway Plugins:
#    - PostgreSQL Database
#    - Redis
#
# 4. Environment Variables to set in Railway:
#    - SECRET_KEY (auto-generated by deployment script)
#    - GITHUB_TOKEN (from GitHub)
#    - GEMINI_API_KEY (from Google AI Studio)
#    - ENVIRONMENT=production
#    - ALLOWED_ORIGINS (your frontend URL)
